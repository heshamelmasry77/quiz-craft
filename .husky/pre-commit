# Make sure local bins & Homebrew bins are available for GUI apps (SourceTree)
export PATH="$PWD/node_modules/.bin:/opt/homebrew/bin:/usr/local/bin:$PATH"

echo "🔍 Running lint-staged..."
lint-staged
