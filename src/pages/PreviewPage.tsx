export default function PreviewPage() {
    return (
        <section>
            <h1 className="text-xl font-semibold">Preview</h1>
            <p className="text-sm text-gray-600">Student view (no scoring yet).</p>
        </section>
    );
}
